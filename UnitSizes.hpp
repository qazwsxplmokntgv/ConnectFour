#pragma once
#ifndef UNIT_SIZES_H
#define UNIT_SIZES_H

namespace UnitSizes {
	constexpr float tokenRadius = 50.f;
	constexpr float outlineThickness = -10.f;
	constexpr float tileSize = 2 * tokenRadius;
	constexpr int sideBarWidth = 2;
}

#endif // !UNIT_SIZES_H
